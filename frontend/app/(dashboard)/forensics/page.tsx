import { ForensicsWorkspace } from "@/components/forensics/forensics-workspace";

export default function ForensicsPage() {
  return (
    <ForensicsWorkspace
      title="INCIDENT FORENSICS"
      description="Deep inspection mode for high-signal alerts. Review AI confidence, drill into payload traces, and validate the autonomous response before escalation."
    />
  );
}
