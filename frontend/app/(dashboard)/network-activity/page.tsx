import { ForensicsWorkspace } from "@/components/forensics/forensics-workspace";

export default function NetworkActivityPage() {
  return (
    <ForensicsWorkspace
      title="NETWORK FORENSICS"
      description="Real-time surveillance of the Autonomous AI Layer. Every transaction, contract interaction, and state change is analyzed for potential security deviations."
    />
  );
}
